version: "3.9"
services:
  finflow-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: finflow-api
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped
